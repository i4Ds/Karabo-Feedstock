name: Pinocchio

on:
  workflow_dispatch:
    inputs:
      label:
        type: string
        default: "main"
        required: true
      dev:
        type: boolean
        required: false
        default: false
      devDeps:
        type: string
        required: false
        default: "PFFT_VERSION, FFTW3_VERSION"
        
  workflow_call:
    inputs:
      label:
        type: string
        required: true
      dev:
        type: boolean
        required: false
        default: false
      devDeps:
        type: string
        required: false
        default: "PFFT_VERSION, FFTW3_VERSION"

jobs:

  Pinocchio:
    uses: ./.github/workflows/build_base.yml
    with:
      packageName: pinocchio
      packageVersion: $PINOCCHIO_VERSION
      label: ${{ inputs.label }}
      dev: ${{ inputs.dev }}
      devDeps: ${{ inputs.devDeps }}
      isNative: true
    secrets: inherit
